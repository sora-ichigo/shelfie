# Backend configuration for staging environment
# Uncomment and configure when ready to use remote state

# terraform {
#   backend "gcs" {
#     bucket = "shelfie-stg-terraform-state"
#     prefix = "api-cloud-run"
#   }
# }
