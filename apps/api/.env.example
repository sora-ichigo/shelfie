# Database Configuration
# ローカル PostgreSQL に shelfie ユーザーとデータベースを作成して接続
# 作成コマンド例:
#   psql -U $(whoami) -d postgres -c "CREATE USER shelfie WITH PASSWORD 'shelfie';"
#   psql -U $(whoami) -d postgres -c "CREATE DATABASE shelfie OWNER shelfie;"
#   psql -U $(whoami) -d postgres -c "CREATE DATABASE shelfie_test OWNER shelfie;"  # テスト用DB
DATABASE_URL=postgres://shelfie:shelfie@localhost:5432/shelfie
DB_POOL_MAX=20
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECTION_TIMEOUT_MS=2000

# Server Configuration
PORT=4000
NODE_ENV=development
LOG_LEVEL=info

# Security Configuration
CORS_ORIGIN=http://localhost:3000
RATE_LIMIT_MAX=100

# Firebase Auth (optional for local development without auth)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=your-client-email@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
