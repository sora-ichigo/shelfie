# Database Configuration
# Local development with Docker Compose (Unix socket - avoids port conflicts):
DATABASE_URL=postgres://shelfie:shelfie@localhost/shelfie?host=/Users/YOUR_USERNAME/path/to/shelfie/apps/api/tmp/pg-socket
# Note: Replace with absolute path to your project's socket directory
# Example: DATABASE_URL=postgres://shelfie:shelfie@localhost/shelfie?host=/Users/ichigo/ghq/github.com/sora-ichigo/shelfie/apps/api/tmp/pg-socket
DB_POOL_MAX=20
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECTION_TIMEOUT_MS=2000

# Server Configuration
PORT=4000
NODE_ENV=development
LOG_LEVEL=info

# Security Configuration
CORS_ORIGIN=http://localhost:3000
RATE_LIMIT_MAX=100

# Firebase Auth (optional for local development without auth)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=your-client-email@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
