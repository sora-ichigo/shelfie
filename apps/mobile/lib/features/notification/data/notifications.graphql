query Notifications($cursor: Int, $limit: Int) {
  notifications(cursor: $cursor, limit: $limit) {
    id
    sender {
      id
      name
      avatarUrl
      handle
    }
    type
    outgoingFollowStatus
    incomingFollowStatus
    followRequestId
    isRead
    createdAt
  }
}
