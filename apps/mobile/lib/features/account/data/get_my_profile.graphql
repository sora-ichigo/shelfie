query GetMyProfile {
  me {
    ... on User {
      id
      email
      name
      avatarUrl
      createdAt
      bookCount
      readingCount
      backlogCount
      completedCount
      interestedCount
    }
    ... on AuthErrorResult {
      code
      message
    }
  }
}
