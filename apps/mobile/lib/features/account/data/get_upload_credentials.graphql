query GetUploadCredentials {
  getUploadCredentials {
    ... on QueryGetUploadCredentialsSuccess {
      data {
        token
        signature
        expire
        publicKey
        uploadEndpoint
      }
    }
    ... on ImageUploadError {
      code
      message
    }
  }
}
