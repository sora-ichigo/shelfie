query BookListDetail($listId: Int!) {
  bookListDetail(listId: $listId) {
    id
    title
    description
    items {
      id
      position
      addedAt
      userBook {
        id
        externalId
        title
        authors
        coverImageUrl
        readingStatus
        source
      }
    }
    stats {
      bookCount
      completedCount
      coverImages
    }
    createdAt
    updatedAt
  }
}
