mutation AddBookToList($listId: Int!, $userBookId: Int!) {
  addBookToList(listId: $listId, userBookId: $userBookId) {
    id
    position
    addedAt
    userBook {
      id
      externalId
      title
      authors
      coverImageUrl
      readingStatus
      source
    }
  }
}
