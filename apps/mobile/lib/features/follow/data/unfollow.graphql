mutation Unfollow($targetUserId: Int!) {
  unfollow(targetUserId: $targetUserId) {
    ... on MutationUnfollowSuccess {
      data
    }
    ... on ValidationError {
      message
      code
    }
  }
}
