mutation RejectFollowRequest($requestId: Int!) {
  rejectFollowRequest(requestId: $requestId) {
    ... on MutationRejectFollowRequestSuccess {
      data {
        id
        senderId
        receiverId
        status
        createdAt
      }
    }
    ... on ValidationError {
      message
      code
    }
  }
}
