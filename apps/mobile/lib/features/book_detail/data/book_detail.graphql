query BookDetail($bookId: String!, $source: BookSource!) {
  bookDetail(bookId: $bookId, source: $source) {
    id
    title
    authors
    publisher
    publishedDate
    pageCount
    categories
    description
    isbn
    coverImageUrl
    amazonUrl
    rakutenBooksUrl
    userBook {
      id
      externalId
      title
      authors
      publisher
      publishedDate
      isbn
      coverImageUrl
      addedAt
      readingStatus
      startedAt
      completedAt
      note
      noteUpdatedAt
      rating
    }
  }
}
