query MyShelfPaginated($input: MyShelfInput) {
  myShelf(input: $input) {
    items {
      id
      externalId
      title
      authors
      coverImageUrl
      readingStatus
      source
      addedAt
      startedAt
      completedAt
      rating
    }
    totalCount
    hasMore
  }
}
